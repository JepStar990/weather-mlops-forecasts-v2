name: Train Daily
on:
  schedule:
    - cron: "17 0 * * *"   # daily 00:17 UTC (right after prune)
  workflow_dispatch: {}

jobs:
  train:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python deps
        run: pip install -r requirements.txt

      - name: Train models
        run: |
          python -m src.jobs.job_train_daily
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
          DAGSHUB_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
          PUBLIC_REPO_NAME: ${{ vars.PUBLIC_REPO_NAME }}
          TARGET_LOCATIONS: ${{ vars.TARGET_LOCATIONS }}
          VARIABLES: ${{ vars.VARIABLES }}
          HORIZONS_HOURS: ${{ vars.HORIZONS_HOURS }}
          
      - name: Promote champion
        run: python -m src.jobs.job_promote_champion
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DAGSHUB_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
          DAGSHUB_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
